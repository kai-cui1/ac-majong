# 01 · 登录与身份

> **模块**：登录与身份（页面 P1）。
> **职责**：定义本模块产品「做什么 / 怎么做」。全局内容见 [00-产品总览.md](./00-产品总览.md)，模块索引见 [README.md](./README.md)。
> **关联**：技术方案 [`../3-技术文档/AC麻将-登录鉴权.md`](../3-技术文档/AC麻将-登录鉴权.md)｜视觉稿 [`../2-效果图/HTML格式高保真原型/login.html`](../2-效果图/HTML格式高保真原型/login.html)｜业务基线 [`../0-业务规则/AC麻将游戏规则说明书.md`](../0-业务规则/AC麻将游戏规则说明书.md)
> **开发状态**：✅ 已开发（M-B：本地 mock 登录 + `users` 落库 + Redis 会话）；M-K 微信接入本地切片已备（云托管 `x-wx-openid` 免 code2Session + WeChatTransport + 平台分支登录 + Dockerfile），余微信构建/真机/合规。

---

## 1. 模块目标

一键进入、身份唯一、合规前置：玩家无需账号密码，微信授权即登录；首次进入完成协议与健康提示确认后方可开局。

## 2. 功能需求（FR）

| 编号 | 需求 | 优先级 | 状态 |
|---|---|---|---|
| FR-登录-01 | 微信授权登录：`wx.login` 获取 code → 后端换 openid 作唯一身份；获取昵称/头像用于展示 | P0 | 🟡 mock 已通；M-K 采用**云托管握手头 `x-wx-openid`** 免 code2Session（WeChatIdentity 已测）；昵称头像暂用默认展示值 |
| FR-登录-02 | 重进自动恢复登录态（免重复输凭据） | P0 | ✅ H5 会话令牌 30 天 / 微信 openid / mock localStorage |
| FR-登录-03 | 合规：首次进入展示《用户协议》《隐私政策》与健康游戏提示，需勾选同意方可登录 | P0 | ✅ |
| FR-登录-04 | H5 路线账号密码登录：注册即登录（账号 3-20 位/密码≥6 位/昵称可选）、scrypt 哈希存储、会话令牌 30 天免密复登、错密/过期明示原因、连错 5 次断开 | P0 | ✅（IDENTITY=account，2026-09-17） |

## 3. 交互与页面（P1）

- 品牌 Logo + 标题「AC 麻将俱乐部」+ 副标题。
- 登录控件按平台三态：微信端「微信一键登录」；Web/H5 账号表单（账号/密码/昵称可选三输入框 + 「登录 / 注册」按钮）；原生壳「一键登录」(mock)。**未勾选协议时置灰不可点**，勾选后变亮。
- H5 有有效会话时进登录页自动免密复登（状态行「自动登录中…」）；失败回表单并明示原因。
- 协议勾选行：《用户协议》《隐私政策》+「已年满 18 周岁」确认。
- 健康游戏提示：虚拟积分不可兑换/提现、适度游戏益脑。
- 版本号。
- **登录状态提示**：登录中…；失败时提示「连接失败，请确认服务端已启动」并恢复按钮。

## 4. 关键流程

```
勾选协议 → 按钮可用 → 微信端点「微信一键登录」/ H5 填表点「登录 / 注册」（首次即建号）
 → 客户端取本地身份(mock) + 上报昵称/头像 → 连接服务端并 auth
 → 服务端校验身份 → users 落库(upsert，刷新 lastLoginAt) + 建会话(Redis，TTL)
 → authOk 回传 { userId, profile } → 进入大厅（用户条显示真实昵称 / ID）
```

## 5. 数据与身份

- **身份（谁）**：openid 为唯一身份，由服务端校验（权威）；客户端不可伪造。
- **资料（展示）**：昵称 / 头像由客户端上报（`UserProfile`）。
- **展示约定（2026-09-19 用户定规）**：玩家之间以**昵称**建立认知——除个人中心类页面（查看自己的 ID）外，**所有场景对他人一律昵称显示、不展示账号 ID**（大厅房间列表/等待页座位/牌桌 pinfo/开局仪式/结算/回放同口径）；Bot 一律显「机器人」。
- **落库**：每次登录 upsert `users`（openid / nickname / avatarUrl / lastLoginAt）。
- **会话**：登录后建立会话（token → openid，TTL），用于鉴权与后续重连恢复（对局恢复 → M-I）。
- 技术细节见 [`../3-技术文档/AC麻将-登录鉴权.md`](../3-技术文档/AC麻将-登录鉴权.md)。

## 6. 合规

- 虚拟积分不可兑现、无赌博暗示（NFR-04，见 [00-产品总览.md](./00-产品总览.md)）。
- 首次进入协议勾选 + 健康提示；未成年人保护（如需）。

## 7. 依赖与边界

- **依赖**：业务基线身份/合规决策；技术方案《登录鉴权》；网络层（连接/传输）。
- **边界（不在本模块）**：真实微信 `wx.login → code2Session`、服务端签发 session token → M-K；断线重连恢复登录态与对局 → M-I（BL-007）。

---

## 维护记录

| 日期 | 概要 |
|---|---|
| 2026-09-16 | 从过渡存根拆出 FR-登录 + P1，并按 M-B 实际实现补全：mock 身份持久化、profile 上报、`users` 落库、Redis 会话、登录状态提示、大厅显示真实资料；标注真实 `code2Session` → M-K、重连 → M-I |
| 2026-09-17 | H5 账号鉴权落地：FR-登录-04 新增、FR-登录-02 语义更新；交互三态登录控件 + 会话免密复登；users.pass_hash + Redis 会话 30 天 |
| 2026-09-17 | 原型回补：login.html 改 H5 账号表单三态（与 FR-登录-04 同批对齐） |
| 2026-09-19 | **昵称展示约定（用户定规）**：§5 增「除个人中心类页面外一律昵称、不展示账号 ID」全局规则；暴露面=等待页座位（RoomScreen.displayName 截断账号→改昵称，依赖 RoomView 昵称透传，随 BL-017 仪式重设计同批落地）、开局仪式卡片 |
