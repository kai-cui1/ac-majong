# 03 · 房间系统

> **模块**：房间创建 / 加入 / 等待页（P5）/ 开始 / 散场解散 / 生命周期。
> **职责**：定义本模块产品「做什么 / 怎么做」。全局见 [00-产品总览.md](./00-产品总览.md)，索引见 [README.md](./README.md)。
> **关联**：入口见 [02-大厅与入口.md](./02-大厅与入口.md)｜对局见 [04-对局与牌桌.md](./04-对局与牌桌.md)｜技术方案 [`../3-技术文档/AC麻将-服务端网关与房间.md`](../3-技术文档/AC麻将-服务端网关与房间.md)、[`../3-技术文档/AC麻将-数据持久化与事件溯源.md`](../3-技术文档/AC麻将-数据持久化与事件溯源.md)｜视觉稿 [`../2-效果图/HTML格式高保真原型/room.html`](../2-效果图/HTML格式高保真原型/room.html)
> **开发状态**：🟢 M-C/M-D/M-G 已实现：服务端建房/加入/座位 + `rooms` 创建落库（含「不限」=0）+ 房号加入校验；高保真等待页 `RoomScreen`（`room.html`：房号卡片/分享位/2×2 座位/头像在线点/房主·我·Bot 标签）+ Bot 陪玩（房主放 1~3 Bot）+ 满员开局 + `room_member_events` 进出落库；散场（达上限自动/房主解散）→ `closeRoom(final_score)`+`status=closed`（M-G）。

---

## 1. 模块目标

以**房间**为对局容器：房主组局、好友入局、满员开局、打满散场。房间号 + 微信分享实现「一键入局、无需匹配」；服务端权威管理座位、开始条件与生命周期。**房间 = 一个完整结算周期（BL-016）**：周期内各玩家在本房的累计积分持久保留（跨退出/重进、跨服务重启），房间关闭时积分定格、玩家据此**线下相互结算**；房间号**全局唯一、永不复用**。

## 2. 功能需求（FR-房间）

| 编号 | 需求 | 优先级 | 状态 |
|---|---|---|---|
| FR-房间-01 | **创建房间**：房主设定**局数上限**（`4`/`8`/`16` 局 / **`不限`**，D-23）→ 生成 **6 位房间号** + 微信分享卡片。**不限**=不设上限、无限续局，直到房主手动解散/退出 | P0 | ✅ 建房 + `maxRounds`（含不限=0）+ `rooms` 落库（M-C，e2e+MySQL 验证）；分享卡片 → M-K |
| FR-房间-02 | **加入房间**：输入房间号或点分享卡片进入；房间**未满 4 人且未开始**时可新加入；**已入座成员在房间周期内随时可重进**（对局中按断线重连处理，累计积分保留，见 FR-房间-09）；已关闭房间拒绝加入并提示「房间已关闭」 | P0 | ✅ 房号加入 + 校验（不存在/已满/已开始）+ 客户端加入弹窗（M-C）；成员重进恢复（含服务重启后事件溯源重建）→ BL-016 |
| FR-房间-03 | **等待页**：4 个座位，展示各座位头像/昵称/在线状态；房主位标识 | P0 | ✅ `room.html` 高保真 `RoomScreen`（房号卡片/分享位/2×2 座位卡片/头像在线点/房主👑·我·Bot 标签/空位虚线框，M-D）|
| FR-房间-04 | **开始条件**：座位**满 4 人**（真人 + Bot 混合皆可，D-22）；房主点「开始」进入对局 | P0 | ✅ 服务端「仅房主 + 满 4 人」守卫 + Bot 计入满员，开局切牌桌（M-D）|
| FR-房间-05 | **散场/解散**：达局数上限自动散场；**「不限」局数时只能由房主手动解散**；散场后**积分定格**（写入 `final_score`，作为线下结算依据；不做账户划拨，D-03/D-32） | P0 | ✅ 达上限 `nextRound`→`finishRoom(maxRounds)` 自动散场；不限局数房主 `dissolve`→`finishRoom(dissolve)`；两者均 `closeRoom(final_score)`+`status=closed`+房间回收（M-G）|
| FR-房间-06 | **生命周期**：对局中禁止新玩家加入；玩家中途退房按断线/托管处理（见 [06](./06-断线重连与托管.md)） | P0 | 🟡 退房保留座位已实现；断线托管 → M-I |
| FR-房间-07 | **房间号全局唯一、永不复用**：发号时对照内存活跃房间 + `rooms` 历史表双重查重；房间关闭后房号不释放（加入已关闭房号提示「房间已关闭」） | P1 | ✅ BL-016：`genId` 双重查重，房号不复用 |
| FR-房间-08 | **Bot 陪玩**：房主在等待页可为空位放入 **1~3 个 Bot**（陪自己或陪真人玩）；Bot 计入满员、可与真人混合开局；**放 Bot 时可为其指定「打法」**（见 [11-AI机器人.md](./11-AI机器人.md) FR-AI-03，P1 开放「新手陪练/最大概率打法」）；未指定时用默认打法。Bot 代打策略详见 [11-AI机器人.md](./11-AI机器人.md)（P1 前为骨架保守代打） | P1 | ✅ `addBot`/`removeBot` 协议 + `RoomActor.addBot/removeBot` + `makeBotConnection` 代打 + `roomView.isBot`（M-D）；🔴 **打法选择入口** → BL-031 P1（待编码）|
| FR-房间-09 | **房间积分周期（BL-016）**：房间内各成员累计积分持久化（每局末写 `rooms.member_scores` 账本）；成员退出再进、服务重启后重进均**恢复原座位与累计积分**（事件溯源重建房间）；直至房间关闭积分定格 | P0 | ✅ BL-016：积分账本 + 重进恢复 + 重启重建 |
| FR-房间-10 | **房间玩法参数（BL-017/BL-020）**：建房时房主设三开关——① 牌墙模式 `wallMode`：**物理牌墙展示 / 随机发牌**（默认物理牌墙展示）；② 摸牌位骰 `breakDice`：开/关（默认开，启用后每局庄家掷骰定开牌点，见 [04](./04-对局与牌桌.md) FR-对局-19/20）；③ **先看吃再碰 `chiFirstView`：开/关（默认开**，启用后吃家选定吃意图全桌公开，见 [04](./04-对局与牌桌.md) FR-对局-21）；④ **时间档（BL-032）**：思考时间 `turnSec` **10/15/20/30 默认 15**、响应时间 `respSec` **5/8/10/15 默认 8**（建房弹层左列分段 pills，服务端钳制到最近档位；服务端权威截止与超时代打见 [04](./04-对局与牌桌.md) FR-对局-13）；**选位仪式为每房标准流程不可关**（FR-对局-18）；参数落 `rooms.settings`、等待页展示；**开局前可由房主在等待页修改（FR-房间-12），开局后锁定不可改** | P1 | ❌ BL-017/BL-020；④ 时间档 ✅ BL-032（2026-09-22） |
| FR-房间-11 | **公开房间与大厅房间列表（BL-018）**：建房增「公开房间」开关（**默认开**）：开=进入大厅「房间列表」可被发现；关=仅房号/分享卡可入（同 FR-房间-02）。大厅列表仅列**公开且未关闭**房（等待+对局中），行字段=房号/房主昵称/人数 n/4/局数/状态徽标；**等待且有空位**可一键加入（同房号加入校验），对局中/满员置灰不可点（观战预留）；排序=等待先于对局中、同组按创建时间倒序，上限 20 行；刷新=进大厅拉取+**5s 自动**+手动刷新钮；列表仅暴露上述字段（无积分/隐私） | P1 | ❌ BL-018（设计完成待用户确认） |
| FR-房间-12 | **等待页房间玩法集中展示与房主修改（2026-09-22 用户新增）**：创建弹层所设的全部房间参数（**局数上限 / 牌墙模式 / 摸牌位骰 / 先看吃再碰 / 公开房间 / 时间档**）在**等待页集中展示**（房号卡摘要 + ⚙ 入口），**房主可在开局前修改**（弹层内 pills/开关，保存后广播全员）；**房间一旦启动游戏则全部锁定不可改**（⚙ 变 🔒、弹层只读）。非房主只读。选位仪式恒开不在此列 | P1 | 🟡 BL-031 P1（Cocos 玩法弹层已落地：局数/牌墙/摸牌位骰/先看吃再碰/公开 可改+开局锁定）；**BL-037（2026-09-22）原型 room.html 玩法弹层补时间档两行（思考 10/15/20/30、响应 5/8/10/15，rp-pill 单选，档位与建房弹层同源）＋房号卡摘要补「思 Ns·响 Ns」；客户端 RoomRulesModal 同批落地（面板 400×340、两行 pills、updateRoom 增传 turnSec/respSec、?rpDemo=1 自检）｜BL-037 闭环 |

## 3. 交互与页面

### 3.1 房间等待页（P5）

- **4 个座位**：空位显示「等待加入…」；已入座显示头像 / 昵称 / 在线状态；**房主位**加标识；**Bot 座位**显示「机器人」标识。
- **房间号**：显著展示 6 位房号 + `复制` / `微信分享` 按钮。
- **局数上限**：显示本房设定（x 局 / 不限）。
- **玩法参数（BL-017/BL-020，FR-房间-10/12）**：房号卡展示本房玩法摘要（牌墙模式/摸牌位骰/先看吃再碰/公开/时间档）；**房主点卡上 ⚙ 打开「房间玩法」弹层**集中修改（局数 pills + 牌墙 pills + 三开关 + 时间档 pills），**开局前可改、开局后锁定（⚙→🔒、只读）**；非房主只读。建房弹层内仍由房主初设（横屏双列弹层：左局数 2×2 + **时间档分段两行（BL-032）**、右开关列）。
- **添加机器人**（仅房主，FR-房间-08）：为空位放入 Bot（1~3 个），可在「等真人」与「补 Bot 开局」间自由组合；真人加入时优先占空位，房主可移除多余 Bot。**放 Bot 时弹出「机器人打法选择面板」**（默认高亮「最大概率打法」，P2~P4 打法锁定预告），确认后座位标签显示「Bot · {打法名}」（详见 [11-AI机器人.md](./11-AI机器人.md) §3.1、§4）。
- **开始按钮**：仅房主可见可点；座位**满 4 人**（真人 + Bot）才可点（未满置灰并提示「等待更多玩家或添加机器人」）。
- **退出**：离开房间返回大厅。

### 3.2 分享卡片

- 微信分享携带房间号，好友点击直接进入该房等待页（免手输）。真实分享 API → M-K。

### 3.3 大厅房间列表（P2 增补，BL-018）

- **面板位置**：大厅两大入口卡下方、「战绩/回放」次要入口上方；头部=标题「公开房间」+ 房数计数 + 手动刷新钮 + 「5s 自动刷新·更新于 hh:mm:ss」。
- **行布局**（单行六段）：房号（mono 金字）｜房主昵称｜人数 `n/4`｜局数（4/8/16/不限）｜状态徽标（**等待**=金绿亮色 / **对局中**=灰）｜右侧动作钮。
- **动作钮**：等待且有空位=「加入」（gold 主色，点击走 FR-房间-02 同套校验入座）；对局中=「对局中」、满员=「已满」，均置灰不可点（观战预留后续迭代）。
- **空态**：「暂无公开房间 · 成为第一个开桌的人」+ 跳转创建弹层按钮。
- **创建弹层增补**：玩法设置区首行增「公开房间」开关（**默认开**），副文案：开启=大厅房间列表可见；关闭=仅房间号/分享卡可入。
- **横屏客户端布局方向**（技术期细化）：左列保留入口卡/次要入口，右列承载房间列表面板（844×390 横屏）。
- **刷新节奏**：进大厅拉取一次 + 5s 自动轮询 + 手动刷新钮；列表期间房间状态变化（满员/开局/关闭）下次轮询自然消失或变徽标。

## 4. 关键流程

```
房主：创建(选局数上限) → 生成房号(全局唯一·永不复用) → 等待页 → 分享邀真人 / 放入 1~3 Bot → 满 4 人(真人+Bot) → 点「开始」→ 对局
玩家：输房号/点分享 → 校验(存在·未满·未开始；已关闭拒绝) → 入座等待页 → 房主开始后进入对局
成员重进：输房号 → 房间在内存→直接恢复座位/积分；不在内存(服务重启)→DB 事件溯源重建后恢复
对局循环 N 局(每局末积分入账本) → 达局数上限 / 房主解散(「不限」时仅此) → 散场总战绩 → 房间关闭(积分定格·线下结算)
```

> 服务端房间状态机（`waiting`/`playing`/`finished`）、座位管理、开始守卫、防透视裁剪的实现见 [`../3-技术文档/AC麻将-服务端网关与房间.md`](../3-技术文档/AC麻将-服务端网关与房间.md)。

## 5. 数据与持久化

- **房间落库**：`rooms`（房号 / 房主 / 局数上限 / 初始·**成员积分账本 `member_scores`**·最终积分 / 状态）+ `room_member_events`（create/join/leave/ready 进出流水）。DDL 见 [`../3-技术文档/AC麻将-数据持久化与事件溯源.md`](../3-技术文档/AC麻将-数据持久化与事件溯源.md) §4。
- **积分账本（BL-016）**：每局末将各家累计积分写入 `rooms.member_scores`；服务重启后由「最新一局快照 + 动作日志（含 Redis 未落盘缓冲）」事件溯源重建房间与积分（FR-房间-09）。
- **玩法参数（BL-017）**：`rooms.settings` JSON（`wallMode`/`breakDice`），建房落库、开局仪式与发牌路径据此分支；选位骰结果落 `rooms.seating` JSON（事件溯源可复现）。
- **局数上限表示**：`4/8/16` 存具体值；**`不限`** 以约定值（如 `0`/`null`）表示，续局时不做上限判定（技术实现见 [`../3-技术文档/AC麻将-服务端网关与房间.md`](../3-技术文档/AC麻将-服务端网关与房间.md)）。
- **接线进度**：登录落库（M-B）、房间创建落库（M-C）、成员进出落库（M-D）、对局事件溯源（M-E）、结算台数明细（M-F）、散场 `closeRoom(final_score)`（M-G）均已接线（见持久化技术方案 §7 BL-013）。
- **积分**：随房间生命周期存在，**跨退出/重进保留**；房间关闭时定格（`final_score`）作为线下结算依据，不做跨房账户（D-03/D-32，详见 [05-结算与积分战绩.md](./05-结算与积分战绩.md)）。

## 6. 依赖与边界

- **依赖**：大厅入口（创建/加入来源）；服务端网关与房间（房间状态机）；持久化（房间落库）；网络层（`create`/`join`/`start`/`leave` 指令与 `RoomView`）。
- **边界（不在本模块）**：局内对局归 [04-对局与牌桌.md](./04-对局与牌桌.md)；散场战绩展示归 [05-结算与积分战绩.md](./05-结算与积分战绩.md)；中途掉线/退房托管归 [06-断线重连与托管.md](./06-断线重连与托管.md)；微信分享 API → M-K。

---

## 维护记录

| 日期 | 概要 |
|---|---|
| 2026-09-16 | 从过渡存根拆出 FR-房间（A.3）+ P5；按服务端已实现的 `roomManager`/`roomActor`（建房/加入/座位/开始守卫/补 Bot/防透视）补「状态」列，标注客户端等待页 UI 与房间落库接线 → M-C/M-D；补充持久化（`rooms`/`room_member_events`）与散场清零（D-03/D-32）引用 |
| 2026-09-16 | M-C 文档先行细化：明确「不限」局数=无限续局至房主解散（FR-房间-01/05）、开始条件含真人+Bot 混合满员（FR-房间-04）；**新增 FR-房间-08 Bot 陪玩模式**（房主等待页放 1~3 Bot，重构自 `devBots` 自动补齐，交互落地 M-D）；补充等待页「添加机器人」交互、`rooms` 落库字段与「不限」技术表示、房间创建落库接线 → M-C |
| 2026-09-16 | M-C 实现回填：服务端 `rooms` 创建落库（含「不限」=`max_rounds:0`、`nextRound` 不做上限判定）+ 房号加入校验（e2e + MySQL 验证：room 落库/加入入座广播/不存在房间被拒）；客户端 `RoomScreen` 等待页雏形。FR-房间-01/02 转 ✅、FR-房间-03 转 🟡；Bot 陪玩（FR-房间-08）+ 成员进出落库 → M-D |
| 2026-09-16 | M-D/M-G 实现回填：开发状态 🟢。M-D——FR-房间-03（`room.html` 高保真 `RoomScreen`）/04（真人+Bot 混合满员开局）/08（`addBot`/`removeBot`+`makeBotConnection` 保守代打）转 ✅ + `room_member_events` 进出落库；M-G——FR-房间-05 转 ✅（达上限 `nextRound`→`finishRoom(maxRounds)` 自动散场、不限局数房主 `dissolve`→`finishRoom(dissolve)`，两者均 `closeRoom(final_score)`+`status=closed`+`RoomManager.remove` 回收）。e2e 双场景散场落库验证、server 35 测试绿 |
| 2026-09-18 | **BL-016 房间积分周期**：模块目标/FR-房间-02/05 重写——房间=完整结算周期，积分跨退出/重进/服务重启保留，关闭时定格作线下结算依据（「积分清零」表述全部修正）；FR-房间-07 改为**房号全局唯一·永不复用**（发号对照 `rooms` 历史表双重查重）；新增 FR-房间-09（`rooms.member_scores` 积分账本 + 重进恢复 + 事件溯源重建）；关键流程补「成员重进」分支；数据与持久化补积分账本说明 |
| 2026-09-18 | **BL-017 立项（文档先行）**：新增 FR-房间-10 房间玩法参数（牌墙模式/摸牌位骰两开关，选位仪式恒开）；等待页展示玩法参数；数据补 `rooms.settings`/`rooms.seating`；对局侧 FR-对局-18/19/20 见 [04](./04-对局与牌桌.md) |
| 2026-09-18 | **FR-房间-10 默认值调整（用户要求）**：牌墙模式默认=物理牌墙展示、摸牌位骰默认=开；home.html 创建弹层两开关同步默认 on；建房弹层还原度复核（副文案左对齐双行、局数单行 4 项、补 modal-hint）见前端基座维护记录 |
| 2026-09-18 | **BL-018 立项（文档先行·设计待用户确认）**：新增 FR-房间-11 公开房间与大厅房间列表（建房「公开」开关默认开；大厅列表面板：行六段字段/加入与置灰规则/排序/20 行上限/5s 自动刷新+手动钮/空态）；§3.3 交互增补；home.html 原型同批增房间列表面板+创建弹层公开开关 |
| 2026-09-18 | **BL-018 实现完成**：协议 roomList 请求/响应 + `RoomSettings.isPublic` + `PublicRoomEntry`；服务端 listEntry/publicRooms（等待优先/同组创建时间倒序/20 行上限）；客户端大厅横屏双列重排+列表面板+建房公开开关；e2e 全过（公开房列表可见可加入/非公开不下发/对局中置灰）；详见网关 §14 与前端基座维护记录 |
| 2026-09-22 | **BL-032 时间档可配置（用户需求「等待时间可配置」·同批同步）**：FR-房间-10 增④时间档（思考 10/15/20/30 默认15、响应 5/8/10/15 默认8，建房弹层左列分段 pills、服务端钳制、开局锁定）；FR-房间-12 参数清单与§3.1 摘要同批补时间档；`home.html` 创建弹层原型同批增两行分段选择；对局侧服务端权威截止见 [04](./04-对局与牌桌.md) FR-对局-13 |
| 2026-09-22 | **BL-031 P1 立项（文档先行·同批同步）**：FR-房间-08 增「放 Bot 时选择打法」语义（默认「最大概率打法」，未上线打法锁定预告），指向新模块 [11-AI机器人](./11-AI机器人.md)；§3.1「添加机器人」补打法选择面板与座位标签「Bot·{打法名}」交互；`room.html` 同批新增打法选择弹层原型。本轮仅文档+原型，**编码未开工** |
| 2026-09-22 | **FR-房间-12 立项（用户新增需求·文档先行）**：等待页**集中展示**创建弹层所设全部房间参数（局数/牌墙/摸牌位骰/先看吃再碰/公开）+ **房主开局前可改**（⚙→「房间玩法」弹层）+ **开局后锁定**（⚙→🔒、只读）；FR-房间-10 补「开局前可改/开局锁定」口径；§3.1 玩法参数交互同批；`room.html` 增玩法参数弹层原型（`?rpDemo=1` 可直开）。**仍待用户复核后编码** |
| 2026-09-22 | **FR-房间-12 Cocos 客户端落地（BL-031 P1 同批）**：新建 `ui/RoomRulesModal.ts`（100% 还原 `room.html` .rp-panel：局数/牌墙 pills + 摸牌位骰/先看吃再碰/公开 switches + 取消/保存）；`RoomScreen` 房号卡右上「⚙」入口（仅房主可见）→ 弹层，保存调 `NetService.updateRoom`（服务端 FR-房间-12 开局锁定校验）；开局后 locked 态（控件置灰、保存禁用）。**时间档编辑未入等待页弹层**（原型未含，仍于建房弹层设定、房号卡摘要展示），FR-房间-12 标 🟡。客户端 typecheck 0 错 |
| 2026-09-22 | **BL-037 时间档入等待页玩法弹层（补 BL-031 遗留缺口·原型先行）**：room.html 玩法弹层增思考/响应时间两行 rp-pill 单选（10/15/20/30 默认15、5/8/10/15 默认8，档位与建房弹层 home.html 同源）＋房号卡摘要补「思 15s · 响 8s」；?rpDemo=1 截图自检通过（面板不溢舞台）；FR-房间-12 状态同批更新；客户端 RoomRulesModal 时间档行待用户确认后编码 |
