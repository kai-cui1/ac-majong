# 08 · 规则页与设置

> **模块**：内置规则说明页（P9）+ 设置（P10）。
> **职责**：定义本模块产品「做什么 / 怎么做」。全局见 [00-产品总览.md](./00-产品总览.md)，索引见 [README.md](./README.md)。
> **关联**：规则内容源 [`../0-业务规则/AC麻将游戏规则说明书.md`](../0-业务规则/AC麻将游戏规则说明书.md)｜入口来自 [02-大厅与入口.md](./02-大厅与入口.md) FR-大厅-03 与牌桌菜单 [04-对局与牌桌.md](./04-对局与牌桌.md) §4｜音效开关服务 [07-表现动画音效.md](./07-表现动画音效.md)｜技术方案 [`../3-技术文档/AC麻将-前端基座与设计系统.md`](../3-技术文档/AC麻将-前端基座与设计系统.md)｜视觉稿 [`../2-效果图/HTML格式高保真原型/rules.html`](../2-效果图/HTML格式高保真原型/rules.html)
> **开发状态**：✅ 已交付（M-H）。规则弹层（大厅+牌桌双入口、分节+番种速查滚动浏览）与设置弹层（静音/退出房间/返回登录/版本协议）已落地并 CDP 截图验证。

---

## 1. 模块目标

让玩家**随时查规则、按需调设置**：内置一份与业务基线一致的《游戏规则》说明，降低 A&C 定制规则的上手门槛；设置页提供音效/静音、退出房间等基础控制。规则页是**只读内容展示**，设置页是**本地偏好 + 房间操作**。

## 2. 功能需求

### 2.1 规则说明（FR-规则页）

| 编号 | 需求 | 优先级 | 状态 |
|---|---|---|---|
| FR-规则页-01 | 内置《游戏规则》说明页，内容取自业务基线（A&C 规则：17 张 / 5 副 / 台数制 / 番种 / 结算） | P1 | ✅（`ui/RulesModal.ts` 分节内容；番种速查取引擎 `PATTERN_TAI` 单一来源）|
| FR-规则页-02 | 可从**大厅**与**牌桌菜单**两处进入；牌桌内进入不中断对局（弹层/暂停态） | P1 | ✅（大厅「规则说明」按钮 + 牌桌右上「规则」钮，弹层形态不中断）|
| FR-规则页-03 | 规则内容分节可浏览（发牌行牌 / 胡牌牌型 / 台数番种 / 结算与连庄 / 末尾规则） | P2 | ✅（五节+附番种速查，ScrollView 滚动）|

### 2.2 设置（FR-设置）

| 编号 | 需求 | 优先级 | 状态 |
|---|---|---|---|
| FR-设置-01 | **音效开关 / 静音**（本地偏好持久化，作用于表现层音效） | P1 | ✅（设置弹层内 🔊/🔇 切换并入统一管理，AudioManager + localStorage 持久化；大厅/牌桌最简钮保留）|
| FR-设置-02 | **退出房间**（对局中退出按断线/托管处理，见 [06-断线重连与托管.md](./06-断线重连与托管.md)） | P0 | ✅（设置弹层「退出房间」→ `net.leave()` 回大厅）|
| FR-设置-03 | 返回登录 / 断开连接（清会话，见 [01-登录与身份.md](./01-登录与身份.md)） | P1 | 🟡 大厅「返回登录」已通（`disconnect`）|
| FR-设置-04 | 版本号 / 用户协议 / 隐私政策查看（合规，呼应登录页） | P2 | ✅（版本号展示 + 协议/隐私占位弹层，合规文案 M-K 替换）|

## 3. 交互与页面

### 3.1 规则说明页（P9）

- **入口**：大厅「规则说明」、牌桌顶部菜单「规则」。
- **形态**：全屏页或弹层（牌桌内用弹层，关闭即回到对局，不中断）。
- **内容**：按业务基线分节展示；文字为主，可配牌型示意（复用牌面素材）。
- **一致性**：内容须与 [`../0-业务规则/AC麻将游戏规则说明书.md`](../0-业务规则/AC麻将游戏规则说明书.md) 及台数计算规格保持一致，规则变更时同步更新（文档先行）。

### 3.2 设置（P10）

- **形态**：弹层（牌桌菜单「设置」或大厅入口）。
- **项**：音效/静音开关、退出房间、返回登录、版本号与协议入口。
- **本地偏好**：静音等偏好持久化到本地（`sys.localStorage`），下次进入沿用。
- **静音开关先行（BL-014）**：完整设置弹层（M-H）落地前，先在大厅用户条与牌桌菜单提供一个 **🔊/🔇 切换按钮**作为最简静音入口：点击即切 `AudioManager` 静音态并写 `sys.localStorage`（key `ac_muted`），启动时读取沿用。待 M-H 设置弹层就绪后，该开关并入设置页统一管理（音效本体与事件映射见 [07-表现动画音效.md](./07-表现动画音效.md) §3.2）。

## 4. 依赖与边界

- **依赖**：业务基线《游戏规则说明书》（规则内容源）；前端基座（`uiModal`/`UiKit` 页面构建）；表现层（音效受静音开关控制）；房间系统/断线托管（退出房间的后续处理）；登录鉴权（返回登录断连）。
- **边界（不在本模块）**：规则本身「是什么」以业务基线为准，本页只做**展示**；音效资源与播放归 [07-表现动画音效.md](./07-表现动画音效.md)；退出后的座位保留/托管归 [06-断线重连与托管.md](./06-断线重连与托管.md)。

---

## 维护记录

| 日期 | 概要 |
|---|---|
| 2026-09-16 | 从过渡存根拆出 P9/P10，并结合 FR-大厅-03（规则入口）补出 FR-规则页 / FR-设置；明确规则内容以业务基线为源、牌桌内进入不中断对局、静音偏好本地持久化、退出房间按断线托管处理；标注未开发（返回登录断连已通）|
| 2026-09-17 | BL-014 游戏音效配套：FR-设置-01 更新为「最简静音开关已落地」（大厅/牌桌 🔊/🔇 切换 + localStorage 持久化），完整设置弹层仍待 M-H；§3.2 补充「静音开关先行」说明 |
| 2026-09-17 | M-H 交付：规则弹层（RulesModal：五节+番种速查、ScrollView、实底底衬）+ 设置弹层（SettingsModal：静音/退出房间/返回登录/版本协议）；大厅与牌桌双入口；牌桌右上按钮簇重排（静音/退出/规则/设置，修复静音钮与✕同坐标重叠）；CDP 截图验证 |
| 2026-09-17 | 原型回补：rules.html 算账节改 D-25 + 弹层形态注记；settings.html 新建 |
