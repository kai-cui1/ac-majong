# 2026 Q3 · 已完成 backlog 条目

| 编号 | 描述 | 分类 | 完成时间 | 关联交付 |
|---|---|---|---|---|
| BL-010 | 后端持久化基座：MySQL 6 表 schema + Redis（会话/快照/动作 Stream）+ Repository 层 | 后端/存储 | 2026-09-16 | 路线图 M-A2：`server/src/persistence/`（`entities` 契约 / `memory` 内存实现 / `mysql` / `redis` / 工厂 `createPersistence` 按 env 可插拔）+ `deploy/docker-compose.yml`（MySQL8+Redis7）+ `deploy/schema.sql`（users/rooms/room_member_events/games/game_initial_states/game_actions 6 表）+ `server/.env.example`。真实 MySQL/Redis 集成测试 3/3 通过：落库一整局（156 条动作）→ 读回 → `replayRound` 还原与实时逐一致 |
| BL-011 | engine 增 `rehydrate`（初始牌墙+手牌）还原入口 + 回放数据接口 | 规则引擎 | 2026-09-16 | 路线图 M-A2：`packages/engine/src/rehydrate.ts` 提供 `snapshotRound` / `rehydrate` / `replayRound`——**算法无关**还原（只存发牌后牌墙+各家起手，seed 仅交叉校验，对应 arch 风险 A8）。4 个单测（快照↔重建恒等、确定性、单动作/整局回放一致）+ server 事件溯源往返测试通过。即 BL-012 回放 UI 的数据接口 |
| BL-002 | 实现局数上限选择（D-23：房主创建时选 4/8/16/不限）+「不限」语义 | 规则/房间 | 2026-09-16 | 路线图 M-C：创建弹窗 4/8/16/不限；「不限」=`maxRounds:0`，`roomActor.nextRound` 改为 `maxRounds>0 &&` 守卫（无限续局至房主解散）；落库 `rooms.max_rounds`。server 24 测试绿（含不限续局 + rooms 落库）|
| BL-004 | 大厅与加入房间流程：创建 / 房间号加入 / 满员开始（替代 autoHost+3Bot） | Cocos客户端 | 2026-09-16 | 路线图 M-C：大厅 `LobbyScreen` 创建/加入弹窗（6 位数字键盘）+ `RoomScreen` 等待页雏形（房号/局数/4 座位/房主开始/退出）；`NetService.createRoom/joinRoom` 返回房间视图；e2e 验证创建/加入/入座广播/房号校验 + MySQL `rooms` 落库。Bot 陪玩（房主主动放）留 M-D |
| BL-003 | 视觉层对齐高保真稿：桌布背景 / 牌背 / 金色徽章 / 浮层样式 / 844×390 精确布局 | Cocos客户端 | 2026-09-17 | 牌桌保真 P0–P2（状态栏单行/西东成组垂直居中/北家不重叠/牌河四区金环/风圈盘）+ 系列视觉修复（子数换行/庄家红徽章/响应框加高/结算明细个数与具体牌/终局摊牌）；844×390 布局预算见前端基座 §11；逐项 CDP 截图验证 |
| BL-005 | 吃牌多解选择器：`doChi` 现取第一个可行顺子，多解时应弹选择 | Cocos客户端 | 2026-09-17 | `showTileChooser` 吃/杠多解共用：面板高度按选项数自适应 + 选项渲染为真实牌面组合；CDP 截图验证（3 选项无溢出） |
| BL-013 | 持久化接入实时对局流程：RoomActor/gateway 在房间创建·成员进出·开局·每动作·结算·关闭时调用 Repository 落库 | 后端/联机 | 2026-09-17 | 随 M-B(users)/M-C(rooms)/M-D(room_member_events)/M-E(games+game_initial_states+game_actions)/M-G(finishRoom→closeRoom) 全流程接线完成；建房→散场 e2e 全量落库验证（6 表）、server 35 测试绿 |
| BL-007 | 断线重连与托管 UI | 联机 | 2026-09-17 | 路线图 M-I：服务端 `playerDisconnected`/`enterTrustee`/`makeTrusteeConnection`（离线 60s→保守代打）+ 重连座位重绑全量恢复；客户端 NetService 自动重连+断线遮罩+pinfo 离线/托管徽章；server 40 测试绿（reconnectTrustee 5 例）+ CDP 截图验证 |
| BL-008 | 规则引擎待复核项（实例级去重 / 一炮多响轮庄 / 八只花摸第 8 花即胡 / N10 门清一摸二） | 规则引擎 | 2026-09-17 | 四项全部收口：实例级见字吸收（INST_ABSORB cap）+ 八只花可选胡/40+成立番（flowers8 降级分解）+ 一炮多响轮庄 D-28 + N10 确认测试；规格书 N10–N13、业务基线 D-26/27/28、engine bl008.test 10 例 |
| BL-012 | 前端对局回放 UI（列表+播放器） | 前端/表现 | 2026-09-18 | 原型 replay-list/replay-player 先行；协议 replayList/replayLoad→replayData（seatNames/viewSeat）；网关 listRoomsByPlayer（UNION 房主+成员）+ D-29 参赛四方校验；ReplayListScreen（房间→局折叠列表）/ReplayPlayerScreen（rehydrate+applyAction 重演、播放/1x2x4x/单步/seek/关键帧/全亮/终局自动摊牌）；server replay.test 2 例；H5 CDP 五截图端到端（含 212 步全量重演） |
| BL-014 | 游戏音效：牌张拟音 + 番种中文语音 + 系统/UI 音（不含 BGM）；含最简静音开关 | Cocos客户端 | 2026-09-17 | AudioManager 单例（playOneShot 并发+resources 懒加载+静音持久化 ac_muted）+ 16 条 mp3 素材 + TableScreen 事件→音效全映射（杠区分明/暗/补）+ uiButton 全局 click + 静音开关并入 M-H 设置弹层 |
| BL-016 | 房间积分周期：积分跨退出/重进/服务重启保留，关闭定格作线下结算依据；房号全局唯一永不复用 | 联机/房间 | 2026-09-18 | PRD03 FR-房间-07/09 + PRD05 FR-积分-06 + result.html 线下结算文案；`rooms.member_scores` 账本（每局末 onGameEnd 落库）+ `genUniqueId` 双重查重 + `rebuildRoomFromStore` 事件溯源重建（快照+game_actions+Redis peekActions→replayRound，座位/积分/回顾恢复，Bot 挂回/真人转托管）+ `RoomActor.restore`；客户端 joinRoom 兼容对局中重进切牌桌 + ResultScreen 线下结算提示；roomScore.test 5 例 + 真实环境重启重进 e2e + CDP 双截图 |
