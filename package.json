{
  "name": "ac-majong",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A&C 麻将 · 微信小游戏（房间制联机、虚拟积分）",
  "scripts": {
    "test": "pnpm -r test",
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "sync:client": "node scripts/sync-to-cocos.mjs"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@10.33.2",
  "engines": {
    "node": ">=20"
  }
}
